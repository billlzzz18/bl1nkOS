# 🛠️ เพิ่มแหล่งข้อมูล (Add Source) - BL1NK Edition

คู่มือสำหรับการเพิ่มแหล่งข้อมูลความรู้ (Knowledge Source) ใหม่ลงใน Knowledge Agent

## ผ่าน Admin UI (แนะนำ)

1. เข้าไปที่หน้า `/admin`
2. คลิก **Add source**
3. กรอกข้อมูลในช่องต่าง ๆ:

### GitHub Source

| Field | Required | Description |
|-------|----------|-------------|
| `id` | Yes | ตัวระบุเฉพาะ (เช่น `my-docs`) |
| `label` | Yes | ชื่อที่ใช้แสดงผล (เช่น `My Docs`) |
| `repo` | Yes | GitHub repository ในรูปแบบ `owner/repo` |
| `branch` | No | Branch ที่ต้องการดึงข้อมูล (ค่าเริ่มต้น: `main`) |
| `contentPath` | No | Path ไปยังโฟลเดอร์เนื้อหา (ค่าเริ่มต้น: `docs`) |
| `outputPath` | No | โฟลเดอร์ปลายทางใน snapshot (ค่าเริ่มต้น: `id`) |
| `readmeOnly` | No | ดึงเฉพาะไฟล์ README.md เท่านั้น (ค่าเริ่มต้น: `false`) |

### YouTube Source

| Field | Required | Description |
|-------|----------|-------------|
| `id` | Yes | ตัวระบุเฉพาะ |
| `label` | Yes | ชื่อที่ใช้แสดงผล |
| `channelId` | Yes | YouTube channel ID (ขึ้นต้นด้วย `UC`) |
| `handle` | No | YouTube handle (เช่น `@MyChannel`) |
| `maxVideos` | No | จำนวนวิดีโอสูงสุดที่จะดึงมา (ค่าเริ่มต้น: `50`) |

4. คลิก **Sync** เพื่อดึงเนื้อหาเข้าสู่ฐานข้อมูลความรู้

## ผ่าน API

```bash
# สร้าง GitHub source
curl -X POST <your-url>/api/sources \
  -H "Authorization: Bearer <admin-api-key>" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "my-docs",
    "type": "github",
    "label": "My Docs",
    "repo": "org/docs-repo",
    "contentPath": "docs"
  }'

# สั่ง Sync ข้อมูล
curl -X POST <your-url>/api/sync \
  -H "Authorization: Bearer <admin-api-key>"
```

## หมายเหตุ (Notes)

- หลังจากเพิ่มแหล่งข้อมูลแล้ว ต้องกด **Sync** ทุกครั้งเพื่อดึงเนื้อหาล่าสุด
- การดึงข้อมูลจาก YouTube จำเป็นต้องตั้งค่า `BL1NK_YOUTUBE_API_KEY` ในไฟล์ `.env`
- แหล่งข้อมูลจะถูกเก็บไว้ใน SQLite และสามารถจัดการผ่าน Admin UI ได้ตลอดเวลา